# Backend base URL. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Force local fixtures and never touch the network. Leave unset for "live, with
# automatic fixture fallback if the backend is unreachable".
# NEXT_PUBLIC_USE_FIXTURES=1
